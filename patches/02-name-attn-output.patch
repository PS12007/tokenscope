diff --git a/src/llama-graph.cpp b/src/llama-graph.cpp
index 8ea441f..86a4f16 100644
--- a/src/llama-graph.cpp
+++ b/src/llama-graph.cpp
@@ -2738,10 +2738,7 @@ ggml_tensor * llm_graph_context::build_attn(
 
     if (wo) {
         cur = build_lora_mm(wo, cur, wo_s);
-    }
-
-    if (wo_b) {
-        //cb(cur, "kqv_wo", il);
+        cb(cur, "kqv_wo", il);
     }
 
     if (wo_b) {
@@ -2850,6 +2847,7 @@ ggml_tensor * llm_graph_context::build_attn(
         } else {
             cur = build_lora_mm(wo, cur, wo_s);
         }
+        cb(cur, "kqv_wo", il);
     }
 
     if (wo_b) {
@@ -2937,6 +2935,7 @@ ggml_tensor * llm_graph_context::build_attn(
         } else {
             cur = build_lora_mm(wo, cur, wo_s);
         }
+        cb(cur, "kqv_wo", il);
     }
 
     if (wo_b) {
@@ -3013,6 +3012,7 @@ ggml_tensor * llm_graph_context::build_attn(
 
     if (wo) {
         cur = build_lora_mm(wo, cur, wo_s);
+        cb(cur, "kqv_wo", il);
     }
 
     if (wo_b) {
@@ -3096,10 +3096,7 @@ ggml_tensor * llm_graph_context::build_attn(
 
     if (wo) {
         cur = build_lora_mm(wo, cur, wo_s);
-    }
-
-    if (wo_b) {
-        //cb(cur, "kqv_wo", il);
+        cb(cur, "kqv_wo", il);
     }
 
     if (wo_b) {
@@ -3167,6 +3164,7 @@ ggml_tensor * llm_graph_context::build_attn(
 
     if (wo) {
         cur = build_lora_mm(wo, cur, wo_s);
+        cb(cur, "kqv_wo", il);
     }
 
     if (wo_b) {
@@ -3222,10 +3220,7 @@ ggml_tensor * llm_graph_context::build_attn(
 
     if (wo) {
         cur = build_lora_mm(wo, cur, wo_s);
-    }
-
-    if (wo_b) {
-        //cb(cur, "kqv_wo", il);
+        cb(cur, "kqv_wo", il);
     }
 
     if (wo_b) {
